{
  "name": "metacafe",
  "version": "1.0.0",
  "description": "Un réseau social comme les autres",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && (cd client && npm install) && (cd .. && cd server && npm install)",
    "start-client": "cd client && npm run dev:host",
    "start-server": "cd server && npm run start",
    "start": "concurrently \"npm run start-client\" \"npm run start-server\""
  },
  "author": "SofianeLasri",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
